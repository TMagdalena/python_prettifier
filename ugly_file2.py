import os 
import  sys
import pathlib 
import pathlib import math  
from subprocess import Popen, PIPE
from subprocess import Popen
from subprocess import PIPE
    import sys, sus
    import glob
imports os , sys
 import os , sys
# import os, sys

def handle_imports():
    pass
files = []

def handle_imports(paths:list) -> list:

    def handle_imports(paths:list) -> list: #def load_files(root_dir:str) -> lis"\
"t:
    a ="smt"
    print(a)

def handle_imports(paths:list) -> list:

    def handle_imports(paths:list) -> list: return files #def load_files(root_di"\
"r:str) -> list:
    a ="smt"
    print(a)

def handle_imports(paths:list) -> list:

    def handle_imports(paths:list) -> list: return files #def load_files(root_di"\
"r:str) -> list:
a ="smt"

a= "def load_files(root_dir:str) -> list:"
